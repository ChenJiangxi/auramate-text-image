---
name: tuwen-archetype-quiz
description: |
  AuraMate 小红书图文 · 图鉴 / 对号入座 / 自测型。10 种形态、12 种剧本、四象限人格，
  让读者在评论区认领自己。互动率和涨粉最高的一类，UGC trigger 最强。
  调用场景：用户说「做个对号入座」「10 种 XX 图鉴」「自测」「你是哪一种」「让评论区动起来」。
  触发词：图鉴 / 对号入座 / 自测 / 你是哪一种 / 类型 / 象限 / 测测 / UGC / 互动。
  前置：先读 auramate-tuwen 的 SKILL.md 和 references/01-redline.md。
allowed-tools: Read, Write, Edit, Bash, Glob, Grep
---

# 图鉴自测 · 让评论区自己动起来

**目的是互动，不是信息。** 这类帖的成败只看一件事：**读者有没有想说「我是 X」的冲动**。

已跑通：post-004「10 种 AI 灵体形态对号入座」（小红书 + 抖音双发）。

## 成立的三个条件

1. **类型数 8–12**。少于 8 不够挑，多于 12 划不完。
2. **每种要有画面感的名字**，不要抽象标签。「发疯日」「捡漏日」赢「伤官型」「偏财型」一万倍。
3. **每种配一句能对号的具体状态**，不是定义。「怪念头、发癫一下」不是「创造力强、表达欲旺盛」。

## 分镜（12 张标准版）

| # | 内容 |
|---|---|
| 1 | 封面：类型总清单 + 大字钩「你今天是哪一种」+ 一个灵体视觉 |
| 2–11 | 每种一张：形态名 + 一句状态 + 灵体视觉 |
| 12 | 收尾：HOW（这是什么做的）/ WHY（为什么会变）+ 开放式互动 |

小红书图文上限 18 张，实际 12 张读完率最好。

**8 张压缩版**：封面 + 每张放 1–2 种 + 收尾。

## 视觉

- 模板 B 浅底卡片风（`templates/card-light/`），一张一个观点，对比强
- 尺寸 1242×1660 出图，GIF 输出按工具的降档结果（通常落在 960px 宽），**全篇统一**
- **每张的灵体必须是真截图**（`~/HermitAgents/auramate-shared/lingti-orbs/` 或 `shishen-mov/` 抽帧）
- 排版跨 slide 完全一致，只换名字和句子 —— 用一个 `_form-template.html` + 脚本批量生成

### ⚠️ 形态图鉴要出 **GIF**，不是静帧

**这类内容的默认交付是动图。** 十种形态本来就是粒子在运动 —— 扒成静帧，等于把内容里
最有意思的那部分丢掉。小红书图文支持 GIF，长按会播。post-004 就是发的 11 张 4 秒循环 GIF，
正文里写「每张图都是实况，长按能看灵体在动」。

**静图只是 fallback**（素材只有截图、或者动图怎么压都超预算时才退回去）。

#### 做法

素材：`~/HermitAgents/auramate-shared/shishen-mov/<形态id>-day.mov`，一形态一段，714×702 60fps。

1. 模板里给要动的容器加 `id="slot"`（`templates/card-light/body.html` 已经有了）
2. 跑工具：
```bash
node tools/slide-gif.js <slide.html> <形态.mov> <out.gif> [秒数=4]
```
它会 Playwright 渲染底图 + 读出 slot 的位置和圆角 → PIL 生成圆角蒙版 → ffmpeg 把录屏
scale/crop 进槽位叠上去 → palettegen 出 GIF。**只有槽位在动，文字和边框逐帧完全静止。**

#### 体积（这类最容易翻车的地方）

单张 ≤ **5.5MB**，全篇 < 50MB（小红书上限 200MB，但太大加载慢、掉完播）。

**动图面积越大越吃体积。** 图鉴的光球是满屏的，1080 宽 / 4s / 10fps 能到 **11MB**。
工具自带降档梯子，会自动往下退并打印退到了哪一档：

```
4s/10fps/192色/1080px → 11.5MB 超预算，降一档
4s/8fps/160色/1000px  → 7.9MB 超预算，降一档
03-tujian-2.gif  5.3 MB  3s @ 8fps · 128 色 · 960px 宽
```

梯子走到底还超，工具会明说 —— 那就得**把 slot 做小**，或者这张退回静图。别硬发一个 20MB 的。

环境变量：`GIF_MAX_MB` 改预算，`GIF_WIDTH` 强制输出宽度（只在做缩略图时用）。

#### 踩过的坑（都已修在工具里）

- `fps` 和 `scale` 必须放在 `overlay` **之后**。放前面输出会跟着底图的 25fps 走，帧数翻 2.5 倍 → 18MB。
- `force_original_aspect_ratio=increase` 缩放有取整误差，跟蒙版差 1px 就 `alphamerge` 报错退出。
  槽位宽高取偶数 + 末尾再 `scale` 一次兜底。
- 圆角常挂在里面的 `img` 上而不是容器上，蒙版要往下找一层读，不然叠上去四角是方的。

## ⚠️ 互动怎么做（这里最容易踩线）

**绝对不能写**：
```
❌ 评论区扣 1
❌ 评论区告诉我你是哪种，我帮你看
❌ 第一个留言的送你 X
❌ 私信我 / 主页点这个
```
这些全是小红书「引导评论 / 引导私信 / 引导跳转」redline。

> post-004 的正文里写了「评论区留你猜的那个名字…第一个留言的，送你今天的形态解读」—— **那是当时的写法，现在不要再用**。用户后来明确定了铁律：不要「评论区扣」类互动诱导话术。

**可以写**（开放式陈述，不是指令）：
```
✅ 你今天是哪一种
✅ 十种全在图里
✅ 我自己最常出现的是顿悟日
```

区别在于：**陈述一个开放问题** vs **要求一个具体动作**。前者一样能激活评论区，后者会被判引导。

## 已有的现成类型体系

| 体系 | 类型数 | 素材 |
|---|---|---|
| 十种灵体形态 | 10 | `shishen-mov/` 10 段录屏 ★ 已做过 |
| 本命色 · 花 · 香 | — | `destined-trio` 功能截图 |
| 人生剧本 | — | `personality-flow` 功能截图 |
| MBTI 命格解析 | 16 | `mbti-personality` ★ MBTI 是天然流量入口 |
| 女性杰出人物对标 | — | `female-archetype` |

**别自己发明类型体系** —— 用产品里真实存在的，否则用户点进去发现没有这个功能，信任崩。

十神 → 形态名对照表见 `tuwen-knowledge-decode/SKILL.md`。

## 标题写法

- 「10 种 AI 灵体形态对号入座（哪个是今天的你）」
- 「我把东方传统里的十种状态做成了 10 个 AI 形态」
- 「你今天是哪一种」

封面大字三行：`东方十种状态` / `10 种 AI 灵体形态` / `你今天 —— 是哪一种？`

## 常见翻车

| 症状 | 修法 |
|---|---|
| 评论区没人说话 | 类型名太抽象，换成有画面的口语 |
| 用户说「灵体不好看」 | 换 orb 素材，或让用户重新生成。别用 CSS 假球 |
| 用户说「下面小字很丑」 | 去掉小字，一张只留形态名 + 一句状态 |
| 被判营销 | 检查是不是写了「评论区扣」类话术 |
