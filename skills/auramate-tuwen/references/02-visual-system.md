# 02 · 视觉系统

## 通用规格（所有模板）

- **尺寸 1242 × 1660**（3:4 portrait，小红书标准）。日签 / 手机截图流可用 1080×1920。
- **一种字体**。靠 weight 拉层级，不靠换字体。
- **字号最多 4 档**。别每个元素一个字号。
- **不加字间空格**，自然断行。
- **全部左对齐**（封面标题可居中，但正文和盒子一律左对齐）。
- 底部固定品牌签 `AURAMATE` / `auramate.net` 或 `灵伴 AI · AURAMATE`。

> 这几条是 2026-07-01 用户连发反馈定下的。原话：「那么多种字体…尽量保持一种」「绕口 / 不整齐」。

---

## 模板 A · 渐变编辑风 ★ 定版主力

**用在**：叙事随笔、知识解析、功能上新、用户故事、节令 —— 也就是绝大多数。

2026-07-01 用户大转向定下的模板（之前是深色风），已跑通 6 篇以上。**默认就用这套，别自己发明。**

### 色 / 字规格

```css
背景  linear-gradient(108deg,#e4dbee 0%,#ebe1ea 38%,#f1e7de 66%,#f6e5d6 100%)
      /* 淡紫 → 暖橘 柔和渐变。不是纯白，不是暖白 */
正文字 #33302e      标题 #1e1824      紫色 accent .em #7a5a8a
红棕 accent（英文词 / 强调）#8a5a52   kicker #9b8ea8   footer #a28f9a
字体  正文 'Noto Serif SC'（宋体，文学感）
      kicker / footer 'Noto Sans SC'
```

### 字号（只有这四档）

| 元素 | 字号 | 说明 |
|---|---|---|
| 封面大标题 | **82**（短标题）/ **66–68**（长标题） | 要大要醒目，手动 `<br/>` 断行别拆词 |
| 内页 head | **52** | 产品图页的说明标题 |
| 正文 | **44** | 所有 slide 统一，line-height 1.9 |
| kicker / footer | **26** | 灰紫小字，算 meta 不算正文 |

> 「正文统一」和「封面标题大」不矛盾：body 统一 44，cover title 单独放大。

### 三种页型

1. **封面页** — kicker + 大标题 + 插画（codex 生成，放下半）+ footer
2. **正文页** — kicker + 2–3 段正文 + footer。**每张写满字**，别一段一张、别大片留白
3. **产品图页** — kicker + head(52) + 产品截图卡 + footer

产品截图卡样式：
```css
border-radius:30px; border:1px solid rgba(90,70,95,0.28);
box-shadow:0 22px 54px rgba(60,40,70,0.28);
/* portrait 手机截图 → height:100% 居中 */
/* landscape / 近方图 → width:100% 或 max-w/max-h:100% */
```

模板文件 → `templates/editorial-gradient/{cover,body,shot}.html`

---

## 模板 B · 浅底卡片结构风

**用在**：图鉴自测、数据背书、结构化拆解 —— 需要「一张图一个观点」的强对比场景。

```css
背景  radial-gradient(125% 80% at 82% 6%, rgba(122,85,166,0.20) 0%, rgba(238,233,245,0) 56%),
      radial-gradient(90% 60% at 8% 96%, rgba(122,85,166,0.12) 0%, rgba(238,233,245,0) 50%),
      #eee9f5
字体  'Noto Sans SC'（黑体，weight 900/700/500）
主色  #2e2740   紫 accent #7a55a6   次级文字 #5f5570   虚线分隔 #c7b8de
```

| 元素 | 字号 |
|---|---|
| 封面大字 | **150**（超短标题，2 行内） |
| 副标题 | **46** |
| 正文 | **44** |
| kicker / footer | **28** |

封面左上角配一个 96px 圆形灵体 badge（**真截图**），旁边写「灵伴 AI · 副标签」。

模板文件 → `templates/card-light/{cover,body}.html`

---

## 模板 C · 深色压字风

**用在**：需要单张强冲击的封面（meme 式）。**已被 A 取代，不做整套**，只在用户明确要「压字风」时用。

```css
背景  radial-gradient(120% 80% at 50% 6%, #241a33 0%, #140f1c 55%, #0d0a12 100%)
字    cream #f5ecd9   紫 accent #c79bec
字体  'Noto Sans SC' 900
封面大字 110–132px，压在截图上方或留白处
产品截图 圆角 20 + border #6b4b8f + 深阴影，铺满宽度
```

模板文件 → `templates/screenshot-caption/cover.html`

---

## 封面规则（所有模板通用）

1. **字要大、铺满画面。** 不要小字挤在顶部三分之一、下面一大片空。用户 2026-06-15 原话：「字大一点，占满整个篇幅」。
2. 3 行排版（每行 4–6 字）约 180px 是不出血的上限；要再大就改 2 行。
3. 标题 ≤ 18 字，超了拆副标题。
4. 关键词用 accent 色 `<span class="em">` 标出来，1–2 个就够。
5. 封面必须有情绪钩子或反预期，不要抽象口号。

## 另有 16 套通用封面模板

需要更多封面风格（提问 / 便签 / 手写 / 弥散 / 书摘 …）→ 全局 skill `~/.claude/skills/xhs-cover-templates/`（16 套已验证模板 + 参考图 + 渲染脚本）。

**用之前必须先看 `references/templates-1.jpg` / `templates-2.jpg` 的真实样图**，不要凭记忆复刻。同一标题建议出 3 个候选让用户挑。

那套 skill 只做**封面**；内页 slide 还是回到本文的模板 A/B + 真实产品截图。
